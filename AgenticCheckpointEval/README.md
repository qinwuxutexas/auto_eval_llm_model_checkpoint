# Deprecated layout

The installable package and scripts now live at the **repository root**:

- `src/agentic_eval/`
- `scripts/`
- `configs/`
- `data/`, `results/`, `demo/`

This folder previously mirrored the Hugging Face dataset path. Prefer the root package.
